import pepita.*
import comidas.*
import niveles.*
import extras.*
import wollok.game.*
import direccion.*

program PepitaGame {
	game.title("Pepita")
	game.cellSize(50)    // Cada celda mide 50px
	game.height(15)
	game.width(15)
	game.boardGround('fondo2.jpg')


 	game.addVisual(silvestre)
	game.addVisual(pepita)
    game.addVisual(manzana)
	
	nivel1.init()
	
	game.start()
}